# Port for the application server
PORT=3001

# Environment (development/production)
NODE_ENV=development

# Absolute path to the data directory containing the SQLite database
# In development, this can be relative like ./data
# In production, use an absolute path
DATA_DIRECTORY=./data
